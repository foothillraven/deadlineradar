{
  "_meta": {
    "purpose": "CPA practice-privilege (mobility) rules, per state, for mobility.ts.",
    "staged_by_design": "Records are added in batches, each with a primary-source citation. A state ABSENT from `records` returns not_verified from the engine, which is the intended behavior, not a gap to fill with guesses. NEVER add a state without a citation just to make coverage look complete -- mobility.ts will refuse to emit a permissive verdict for an uncited row anyway (requireCitationOrDowngrade), so an uncited entry buys nothing and risks a wrong answer.",
    "evidence_standard": "Same as data/cpa_deadlines.json: prefer BOTH the state board's own page (source_url) AND the codified statute/administrative rule (citation + citation_url). `confidence` records which was achieved -- dual_source, single_source, or unverified. Any field that could not be verified is null with an explanatory data_gap_note; null NEVER means 'not required'.",
    "liability_note": "A wrong permissive answer here means a CPA practices without authority. Conservative default is mandatory: when in doubt, leave it null.",
    "as_of_date": "2026-07-30",
    "batch_status": "Batch 1 research returned. Illinois has a full primary-source record but is IN FLUX (see above) and must ship flagged, not clean. Texas/California/New York/Florida findings were prose-only in that pass and need their records finalised before commit. NOTHING committed yet -- the engine degrades honestly to not_verified for every state meanwhile.",
    "schema_note_2026_07_30": "equivalence_test is REQUIRED and cannot be inferred. Primary-source research found the states are actively diverging on what substantial equivalence even means: Texas abandoned NASBA state-level equivalency for an individual-criteria test (SB 522, eff. 2025-09-01); Illinois adopted NASBA NQAS state-level equivalency (P.A. 104-0228, eff. 2026-01-01); New York moves to individual criteria 2026-11-21. A single boolean 'is the home state substantially equivalent?' encodes the WRONG question for whichever kind a state does not use, so the engine refuses to interpret the user's attestation until this field is set.",
    "verification_ttl": "Rows EXPIRE after MOBILITY_VERIFICATION_TTL_DAYS (180) and expiry downgrades to not_verified rather than merely warning. Four of five priority states changed or will change mobility rules inside a 14-month window (2025-09-01 to 2026-11-21), so treating verification as durable would make this dataset confidently wrong within months. Put known changeover dates in rule_changes_on.",
    "rule_in_flux": "Set true when primary sources DISAGREE or a change is mid-flight, and the engine will decline to answer for that state entirely. Illinois is the live example: enrolled P.A. 104-0228 and the compiled ILCS text state different tests for the same section, and the compiled text cites the very act that contradicts it. Do not resolve such a conflict in the data -- flag it and let the user confirm with the board."
  },
  "records": []
}
